class OneHotEncoder:
    def __init__(self, padder:str, max_length: int):
        self.padder = padder
        self.max_length = max_length

        self.alphabet = None
        self.char_to_index = {}
        self.index_to_char = {}

    def add (self, layer)

    def fit (self, data:list):

        self.char_to_index[epoch] = {'characters': characters, 'integers': integers}